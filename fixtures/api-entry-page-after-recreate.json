{
  "recorded_from": "docker.io/louislam/uptime-kuma@sha256:917318f9d7be5257f43ba412c766a473be336eb451d70744f3b482d0c3997c0e",
  "note": "Taken from a container that was set up, removed entirely with `docker rm -f`, and created again on the same mount at the declared config_path. The same sequence with the configuration directory at /config answers {\"type\":\"setup-database\"} instead, because the database was in the container's writable layer and went with it.",
  "request": {
    "method": "GET",
    "path": "/api/entry-page"
  },
  "response": {
    "status": 200,
    "headers": {
      "content-type": "application/json; charset=utf-8"
    },
    "json": {
      "type": "entryPage",
      "entryPage": null
    }
  }
}
